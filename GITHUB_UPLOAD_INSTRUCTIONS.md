# رفع Phase 1-C2 Fix 1 إلى GitHub

## اسم الفرع

```text
phase-1c2-fix1-strict-lesson-scope
```

## رسالة الحفظ

```text
fix: lock generation to selected lessons and add library exports
```

## الرفع والدمج

1. أنشئ الفرع أعلاه من آخر `main`.
2. ارفع محتويات حزمة **الملفات المعدلة فقط** إلى جذر المستودع مع الاستبدال.
3. انتظر GitHub Actions حتى تصبح خضراء.
4. ادمج الفرع في `main`.
5. انتظر اكتمال GitHub Pages.

## إعادة نشر وظيفة Supabase

هذه المرحلة تعدّل التحقق الخادمي ونطاق التوليد، لذلك بعد الدمج:

1. افتح في GitHub:
   `supabase/functions/generate-source-questions/index.ts`
2. اضغط **Raw** وانسخ الملف كاملًا.
3. افتح Supabase ثم وظيفة `generate-source-questions` من المحرر.
4. استبدل الكود القديم كاملًا.
5. اضغط **Deploy function**.

## الاختبار بعد النشر

1. نفّذ `Ctrl + F5`.
2. افتح المسودة المتأثرة السابقة.
3. من الطبيعي أن تعود إلى خطوة المحتوى لأن مراجعها القديمة أُبطلت حفاظًا على صحة الاختبار.
4. اختر دروس النشاط الإشعاعي نفسها، ثم انتقل إلى الخطة والتوليد.
5. تأكد أن البدائل تتناول النشاط الإشعاعي فقط، ولا تسأل عن اسم الوحدة أو مكان الموضوع في الكتاب، ولا تنتقل إلى الضغط أو الدوائر.
6. اختر أحد البدائل واضغط **توليد ثلاثة بدائل مشابهة لهذه المفردة**؛ يجب أن تبقى الصياغات في الفكرة نفسها.
7. افتح **اختباراتي** وتأكد من ظهور:
   - معاينة المسودة المكتملة أو الاختبار المعتمد.
   - Word وPDF لورقة الطالب.
   - Word وPDF لنموذج الإجابة.

لا SQL جديد، ولا أسرار جديدة، ولا تغيير لمفتاح Gemini، ولا إعادة فهرسة للكتاب.
