# تعليمات رفع Phase 2-A — محرك تصميم الاختبار الكامل V2

تُرفع الحزمة فوق نسخة واثق **Phase 1-D2 Fix 1** بالإصدار `0.0.49`.

## اسم الفرع

```text
phase-2a-whole-exam-generation-v2
```

## رسالة الحفظ

```text
feat: add whole exam generation v2
```

## الخطوات

1. أنشئ الفرع من `main` بعد التأكد أن الإصدار الحالي `0.0.49`.
2. فك حزمة `changed_files_only`.
3. ارفع محتوياتها إلى جذر المستودع مع الاستبدال.
4. لا ترفع ملف ZIP نفسه.
5. انتظر GitHub Actions، والنتيجة المطلوبة:

```text
345 passed
0 failed
```

6. بعد اللون الأخضر ادمج الفرع.
7. أعد نشر الملف التالي في Supabase Edge Functions:

```text
supabase/functions/generate-source-questions/index.ts
```

8. نفذ `Ctrl + F5` بعد اكتمال نشر GitHub Pages.

## اختبار القبول الحي

- أنشئ **اختبارًا قصيرًا جديدًا** لا يتجاوز 12 مفردة.
- اترك طريقة التوليد على **تصميم الاختبار كاملًا**.
- تحقق أن واثق يعرض سؤالًا نهائيًا واحدًا لكل مفردة ويختاره تلقائيًا.
- راجع تنوع المهارات والسياقات والبيانات والرسوم في الاختبار كاملًا.
- جرّب تجديد مفردة واحدة، وتأكد أن بقية الاختبار لا تتغير.
- جرّب اختيار **المحرك السابق** وتأكد أنه ما زال يعمل.
- صدّر Word وPDF وتأكد من سلامة التصدير.

## لا يلزم

- لا SQL.
- لا سر جديد.
- لا إعادة فهرسة.
- لا تغيير في نموذج الصور.

## قيد المرحلة

الاختبارات التي تزيد على 12 مفردة تستخدم المحرك السابق مؤقتًا. لم يتوافر اتصال حي بمفتاح Gemini ومشروع Supabase داخل بيئة التجهيز، لذلك أول اختبار قصير بعد إعادة نشر Edge Function هو اختبار القبول النهائي.
