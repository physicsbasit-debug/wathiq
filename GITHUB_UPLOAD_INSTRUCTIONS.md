# رفع Phase 0-G Fix 2A عبر GitHub

1. أنشئ فرعًا من `main` باسم `fix/phase-0-g-arabic-quality-gate`.
2. ارفع محتويات حزمة الملفات المعدلة فقط.
3. رسالة الالتزام: `fix: منع فهرسة النص العربي المشوه في Phase 0-G`.
4. افتح Pull Request إلى `main` ولا تدمج قبل نجاح الفحص.
5. لا يوجد SQL جديد، ولا تعديل على Edge Function، ولا تعديل على `pages.yml`.
6. بعد الدمج والنشر افتح إدارة المحتوى واضغط `Ctrl + F5`.
7. المصدر القديم المشوه يُحوَّل تلقائيًا إلى `يحتاج OCR` وتُحذف مقاطعه القديمة؛ ثم يمكن الضغط على إعادة الفحص للتأكد عمليًا.
