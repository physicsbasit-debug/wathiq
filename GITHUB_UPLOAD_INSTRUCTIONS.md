# تعليمات رفع Phase 1-D2 — جودة القياس وتنوع السياقات

تُرفع الحزمة فوق نسخة واثق المستقرة **Phase 1-D1** بالإصدار `0.0.47`.

## اسم الفرع

```text
phase-1d2-assessment-quality-context-diversity
```

## رسالة الحفظ

```text
feat: improve assessment quality and context diversity
```

## الرفع والدمج

1. أنشئ الفرع من `main` بعد التأكد أن الإصدار الحالي `0.0.47`.
2. فك حزمة `changed_files_only`.
3. ارفع **محتوياتها** إلى جذر المستودع مع الاستبدال، ولا ترفع ZIP نفسه.
4. انتظر GitHub Actions. النتيجة المطلوبة:

```text
328 passed
0 failed
```

5. لا تدمج إذا فشل أي اختبار.
6. بعد اللون الأخضر ادمج الفرع وانتظر GitHub Pages.

## إعادة نشر Supabase

بعد الدمج فقط، أعد نشر الملف كاملًا داخل وظيفة `generate-source-questions`:

```text
supabase/functions/generate-source-questions/index.ts
```

## لا يلزم

- لا SQL.
- لا سر جديد.
- لا إعادة فهرسة.
- لا تغيير لمفتاح Gemini أو نموذج الصور.

## اختبار القبول بعد النشر

1. نفذ `Ctrl + F5`.
2. أنشئ **اختبارًا جديدًا**؛ المسودة القديمة لن تتحول سحريًا إلى اختبار أفضل، فالبرمجيات لم تصل بعد إلى غسل الماضي.
3. اختر درسَي عزم القوة والاستقرار مثلًا.
4. تحقق من وجود تنوع حقيقي، مثل: باب، أرجوحة، مفتاح ربط، جدول بيانات، استقصاء، وحساب.
5. تأكد أن الرسم نفسه لا يتكرر على عدة صفحات بلا ضرورة.
6. افحص أن السؤال الحياتي يحتاج السياق فعلًا، وليس قصة يمكن حذفها.
7. جرّب Word وPDF وتأكد من بقاء الرسوم واضحة ومضغوطة.
8. اختبر صورة AI في مشهد مؤهل، وتأكد أن المشهد يطابق نوعه ولا يعيد تكوينًا سابقًا.

## شرط القبول النهائي

لم تتوافر مفاتيح تشغيل حية في بيئة التجهيز؛ لذلك لا تعتمد المرحلة تشغيليًا قبل نجاح اختبار جديد فعلي بعد النشر، مع بقاء GitHub Actions خضراء.
