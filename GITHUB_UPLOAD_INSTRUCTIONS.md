# رفع Phase 1-C2 إلى GitHub

## اسم الفرع

```text
phase-1c2-exam-assembly-approval-export
```

## رسالة الحفظ

```text
feat: assemble approve and export completed exams
```

## الرفع والدمج

1. أنشئ الفرع بالاسم أعلاه من `main`.
2. ارفع محتويات حزمة **الملفات المعدلة فقط** إلى جذر المستودع مع الاستبدال. الحزمة تراكمية ومبنية فوق ملف `main` الذي رفعته بإصدار 0.0.34، لذلك تحفظ محرك C1 وإصلاحاته وتضيف C2 في خطوة واحدة.
3. انتظر GitHub Actions حتى تصبح خضراء.
4. ادمج الفرع في `main`.
5. انتظر اكتمال GitHub Pages.

## إعادة نشر وظيفة Supabase

هذه المرحلة تعدّل وظيفة التوليد لتنويع الرسومات الجديدة، لذلك بعد الدمج:

1. افتح في GitHub:
   `supabase/functions/generate-source-questions/index.ts`
2. اضغط **Raw** وانسخ الملف كاملًا.
3. افتح Supabase ثم وظيفة `generate-source-questions` من المحرر.
4. استبدل الكود القديم كاملًا.
5. اضغط **Deploy function**.

## الاختبار بعد النشر

1. نفّذ `Ctrl + F5`.
2. افتح اختبارًا مكتملًا أو أنشئ اختبارًا جديدًا.
3. تأكد من توزيع أسئلة الاختيار من متعدد بين الأسئلة الأخرى.
4. راجع اختلاف الرسومات بين المفردات البصرية.
5. اضغط **اعتماد الاختبار**.
6. جرّب:
   - ورقة الطالب Word (.doc).
   - ورقة الطالب PDF / طباعة.
   - نموذج الإجابة Word (.doc).
   - نموذج الإجابة PDF / طباعة.
7. جرّب **إلغاء الاعتماد للتعديل** وتأكد من بقاء الأسئلة.

لا SQL جديد، ولا أسرار جديدة، ولا إعادة فهرسة، ولا تغيير لمفتاح Gemini.
