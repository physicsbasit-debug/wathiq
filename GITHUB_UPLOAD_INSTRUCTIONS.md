# تعليمات رفع Phase 1-D1 — النظام الهجين المنضبط للمرئيات

هذه الحزمة تُرفع فوق نسخة واثق المستقرة **Phase 1-C4 Fix 1** بالإصدار `0.0.46`.

## اسم الفرع

```text
phase-1d1-controlled-hybrid-visuals
```

## رسالة الحفظ

```text
feat: add controlled hybrid scientific visuals
```

## الرفع والدمج

1. أنشئ الفرع أعلاه من `main` الحالية بعد التأكد أن الإصدار فيها `0.0.46`.
2. فك حزمة `changed_files_only`.
3. ارفع **محتويات الحزمة** إلى جذر المستودع مع الاستبدال.
4. لا ترفع ملف ZIP نفسه.
5. انتظر GitHub Actions؛ النتيجة المطلوبة:

```text
320 passed
0 failed
```

6. لا تدمج الفرع إذا ظهر أي اختبار فاشل.
7. بعد ظهور اللون الأخضر، ادمج الفرع في `main` وانتظر اكتمال GitHub Pages.

## إعادة نشر وظيفة Supabase

بعد الدمج فقط:

1. افتح الملف:

```text
supabase/functions/generate-source-questions/index.ts
```

2. انسخ محتواه كاملًا.
3. افتح وظيفة `generate-source-questions` في Supabase.
4. استبدل الكود كاملًا ثم اضغط **Deploy function**.

## لا يلزم

- لا SQL.
- لا إعادة فهرسة للمصادر.
- لا سر جديد.
- لا تغيير لمفتاح Gemini الحالي.

متغير اختياري فقط:

```text
GEMINI_IMAGE_MODEL
```

لا تضفه في الاختبار الأول؛ اترك القيمة الافتراضية الموجودة في الكود حتى لا نصنع مشكلة بإبداع إداري غير مطلوب.

## اختبار القبول بعد النشر

1. نفذ `Ctrl + F5`.
2. أنشئ اختبارًا جديدًا حتى يستخدم عقد التوليد `ai-15`.
3. تحقق أن خيار **تحسين المشاهد المؤهلة بصور 2D مدققة** مفعّل.
4. اختبر مشهد كهرباء ساكنة مناسبًا:
   - يجب أن يبقى الرسم الحتمي موجودًا.
   - إذا نجحت الصورة المدققة، تظهر فوقه.
   - يجب ألا تحتوي الصورة المولدة على نصوص أو أرقام أو أسهم.
5. اختبر مخطط قوى أو دائرة كهربائية:
   - يجب أن يبقى حتميًا، ولا يظهر له زر توليد صورة حرة.
6. صدّر Word وPDF وتأكد من ظهور الصورة أو الرجوع إلى SVG دون فراغ.
7. عطّل خيار الرسوم الهجينة وتأكد أن الرسوم الحتمية تستمر دون حذف الأسئلة.
8. جرّب إعادة توليد صورة ثم العودة إلى **الرسم الحتمي فقط**.

## شرط الاعتماد النهائي

لم تتوافر في بيئة التجهيز مفاتيح تشغيل حية لـGemini وSupabase. لذلك لا تعد المرحلة مقبولة تشغيليًا بالكامل إلا بعد نجاح أول صورة فعلية بعد النشر، مع بقاء GitHub Actions خضراء.
