# Phase 2-C3 — Durable Visual Asset Pipeline

## الإصدار

`0.0.55`

## الهدف المعماري

اقتلاع المسار القديم الذي كان يجعل توليد الصورة تحسينًا اختياريًا داخل المتصفح، واستبداله بمنظومة مهام دائمة مرتبطة بالمسودة والمفردة في Supabase.

## ما تغير جذريًا

- أضيف جدول `question_visual_jobs` لحفظ حالة كل أصل بصري وطلبه ومحاولاته ونتيجته.
- أضيفت Edge Function مستقلة باسم `question-visual-jobs` لإنشاء المهام وتشغيلها في الخلفية واستعادتها بعد الانقطاع.
- لم يعد المتصفح ينتظر توليد الصورة في طلب طويل؛ يرسل مهمة قصيرة ثم يتابع حالتها.
- حالات المهام أصبحت: `queued`, `generating`, `validating`, `ready`, `retry_pending`, `failed`, `cancelled`.
- أي صورة مطلوبة لا تُعد جاهزة إلا إذا كان سجل المهمة `ready` والأصل المربوط يطابق المفردة ونمط العرض.
- تغيير السؤال المختار أو إعادة توليد المفردة يبطل الأصل السابق فورًا وينشئ مهمة جديدة ببصمة جديدة.
- إعادة التوليد تمرر مسار الأصل السابق لحذفه بعد نجاح البديل.
- إذا اكتملت صورة بعد استبدال المهمة أو إلغائها، تُحذف من Storage بدل ترك أصل يتيم.
- مخططات القوى تستخدم `overlay`: صورة 2D للكائن مع طبقة أسهم وقيم حتمية من واثق.
- المشاهد المؤهلة تستخدم `replace`: أصل 2D مباشر بدل الرسم الخطي الاحتياطي.
- لا اعتماد ولا تصدير قبل اكتمال الأصول المطلوبة.
- التصدير يتحقق من الوصول الفعلي إلى ملفات الصور ونوعها قبل Word/PDF.
- Word يدمج صورة الأساس وطبقة الشرح في PNG واحدة.
- المهام الكبيرة تُقسم إلى دفعات، دون إسقاط المرئيات التي تتجاوز 20 مفردة.

## الملفات الجديدة الأساسية

- `supabase/phase_2_c3_visual_asset_jobs.sql`
- `supabase/functions/question-visual-jobs/index.ts`
- `src/visual-jobs.ts`
- `tests/phase-2-c3-durable-visual-jobs.test.mjs`

## الفحوص المنفذة

- `npm run check`: **PASS**
- TypeScript build: **PASS**
- اختبارات Node: **406 passed, 0 failed**
- فحص TypeScript مستقل للـEdge Function الجديدة: **PASS**
- تشغيل `dist` محليًا: **HTTP 200**
- فحص الحزمة من ملفات الأسرار و`.env` و`.git` و`node_modules`: **PASS**

## ما لم يُختبر حيًا في بيئة التجهيز

لم يتوفر اتصال بمشروع Supabase الحقيقي أو Gemini Image، لذلك أول مهمة صورة بعد تنفيذ SQL ونشر الوظيفة الجديدة هي اختبار القبول التشغيلي. لا يُعد ذلك نقصًا في البناء المحلي، لكنه حد لا يجوز تزيينه بعبارات احتفالية فارغة.
