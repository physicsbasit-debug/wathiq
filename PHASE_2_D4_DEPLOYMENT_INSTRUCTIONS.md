# تعليمات نشر Phase 2-D4

## 1. شرط البداية

يجب أن يكون المستودع مبنيًا فوق Phase 2-D3، وأن تكون نتيجة المرحلة السابقة خضراء. كما يجب أن تكون منظومة D2 قد اجتازت اختبار القبول التشغيلي.

## 2. رفع GitHub

أنشئ فرعًا باسم:

```text
feat/phase-2d4-progressive-generation-ui
```

فك حزمة الملفات المعدلة، ثم ارفع **محتويات مجلد `wathiq-main`** إلى جذر المستودع مع الاستبدال.

انتظر GitHub Actions. النتيجة المطلوبة:

```text
492 passed
0 failed
```

بعد نجاحها ادمج الفرع إلى `main` واترك GitHub Pages يعيد النشر.

## 3. متطلبات Supabase

لا يوجد SQL جديد في D4، ولا توجد Edge Function جديدة.

تأكد فقط من وجود الوظيفتين السابقتين منشورتين:

```text
assessment-generation-jobs
assessment-generation-worker
```

إذا لم تكن وظيفة D3 منشورة بعد، انشرها من:

```text
supabase/functions/assessment-generation-worker/index.ts
```

وبالإعداد:

```toml
[functions.assessment-generation-worker]
verify_jwt = false
```

لا تعِد نشر الوظائف التالية بسبب D4:

```text
generate-source-questions
question-visual-jobs
assessment-generation-jobs
```

ولا تنفذ SQL D2 مرة أخرى.

## 4. تحديث المتصفح

بعد اكتمال نشر GitHub Pages:

```text
Ctrl + F5
```

ثم سجل دخول مالك المنصة وانتظر اكتمال مزامنة المصادر.

## 5. اختبار القبول الحي

استخدم اختبارًا جديدًا أو نسخة غير مكتملة، ثم تحقق بالترتيب:

1. بعد إعداد الخطة والضغط على التالي تنتقل الواجهة فورًا إلى صفحة التقدم.
2. يظهر عداد المكتمل والنشط والمنتظر والمتعذر.
3. لا تظهر أكثر من مفردتين في مراحل التشغيل النشطة في اللحظة نفسها.
4. يظهر أول سؤال مكتمل قبل اكتمال بقية الأسئلة.
5. حدّث الصفحة أثناء وجود مفردات قيد العمل.
6. افتح المسودة نفسها بعد التحديث؛ يجب أن تبقى المفردات الجاهزة وتستكمل المنتظرة فقط.
7. إن تعذرت مفردة، استخدم زر إعادة المحاولة الخاص بها؛ لا يجب أن تختفي الأسئلة المكتملة أو يعاد توليدها.
8. بعد اكتمال الجميع، راقب بدء الأصول البصرية المحسنة بوصفها مرحلة لاحقة.
9. انتقل إلى المراجعة، ثم تحقق من Word وPDF والاعتماد.

## 6. ما لا يجب فعله

- لا تنفذ SQL جديدًا.
- لا تعِد فهرسة الكتب.
- لا تعِد رفع المراجع.
- لا تضف سرًا جديدًا.
- لا تنشر `generate-source-questions`.
- لا تختبر المسار القديم أو تختَر محركًا يدويًا؛ خيار المحرك أزيل من المسار الإنتاجي.

## 7. عند ظهور خطأ

احتفظ بالنص الكامل للرسالة ورمز التتبع، وصوّر لوحة التقدم كاملة. لا تنشئ دورة جديدة قبل تسجيل حالة المفردات؛ الهدف من D4 أن نعرف بالضبط أين توقف المسار، لا أن نمسح آثار الجريمة الرقمية ونبدأ من الصفر.
