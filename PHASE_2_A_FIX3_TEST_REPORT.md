# Phase 2-A Fix 3 — تقرير الفحص

## النطاق

إصلاح رفض الأسئلة البصرية بسبب غياب عبارة الإحالة إلى الرسم فقط، مع إضافة إحالة عربية حتمية مناسبة لنوع `fixedVisual` قبل التحقق النهائي.

## اختبارات مضافة ومحدثة

- إضافة إحالة تلقائية لمخطط القوى والجدول والرسم البياني.
- عدم تكرار الإحالة إذا ظهرت في السؤال أو المثير مسبقًا.
- الحفاظ على السؤال النصي دون تغيير عندما لا يوجد رسم.
- اختبار تكاملي كامل عبر `validateAndHydrateGeneratedPayload` يثبت قبول السؤال البصري وإعادة النص المطبّع.
- تحديث اختبارات Edge Function التاريخية لتغطي مطبع الإحالة المدمج داخل الوظيفة.
- فحص TypeScript تركيبي لملف Edge والوحدة المساعدة: صفر أخطاء تركيبية.

## النتيجة

```text
361 passed
0 failed
```

## فحوص إضافية

- البناء ناجح.
- تشغيل `dist` محليًا يعيد HTTP 200.
- اختبار overlay الفعلي فوق Phase 2-A Fix 2 ناجح.
- اختبار النسخة الكاملة بعد فكها ناجح.
- لا أسرار، ولا `.env`، ولا SQL، ولا تعديل على GitHub workflows.
