# Phase 2-D2 — تقرير منظومة التوليد الدائمة

## النتيجة

أُنجزت طبقة التخزين الدائم وإدارة دورات التوليد ومهام المفردات، دون ربطها بمسار الإنتاج الحالي ودون استدعاء Gemini.

## ما أضيف

### قاعدة البيانات

- `assessment_generation_runs` لدورات التوليد.
- `assessment_generation_items` لمهام المفردات.
- علاقات مركبة تمنع خلط المالك أو المسودة أو الإزاحة أو الخطة بين الدورة والمهمة.
- RLS مفروض مع منع الوصول المباشر من المتصفح.
- إدخال ذري ومتكرر بأمان.
- حجز و`lease_token` ونبض واستعادة.
- حماية من النتائج القديمة عبر الإزاحة وبصمة العقد والحجز.
- إلغاء واستئناف وإعادة محاولة محكومة.
- فصل محاولة النقل عن محاولة المحتوى.
- تحسين تجميع حالة الدورة لتجنب إعادة مسح المهام في كل مرحلة داخلية.

### Edge Function

أضيفت وظيفة مستقلة باسم:

```text
assessment-generation-jobs
```

وتدعم إنشاء الدورة وقراءتها وإلغاءها واستئنافها وإعادة المفردة الفاشلة. تتحقق من المستخدم ومن البصمات والعقود والحقول قبل مخاطبة قاعدة البيانات.

لا تستدعي الوظيفة:

- Gemini.
- `generate-source-questions`.
- `generateWholeExam`.
- المحرك السابق.

### العميل

أضيفت خدمة `AssessmentGenerationJobService` دون ربطها بـ`app.ts`، حتى لا يتغير تشغيل واثق قبل اكتمال العامل الجديد.

### اختبار قبول Supabase

أضيف اختبار بعد النشر يعمل داخل معاملة قابلة للتراجع، ويختبر المخطط والصلاحيات والإدخال المتكرر والحجز والنبض والاستعادة ورفض النتائج المتأخرة وحدود المحاولات.

## نتائج الفحص المحلي

- `npm run build`: ناجح.
- `npm run check`: **475 passed, 0 failed**.
- فحص TypeScript الصارم لوظيفة Edge: ناجح.
- تشغيل `dist` محليًا: **HTTP 200**.
- توحيد اختبارات حد الإصدار التاريخية عبر `tests/version-assertions.mjs` حتى لا تتطلب إضافة رقم الإصدار يدويًا إلى عشرات الملفات في كل مرحلة لاحقة.
- لا يوجد ربط في `src/app.ts` بالوظيفة الجديدة.
- لا يوجد استدعاء لـGemini أو المحرك السابق داخل الوظيفة الجديدة.

## ما لم يمكن التحقق منه محليًا

لم يتوفر اتصال بمشروع Supabase الحقيقي أو PostgreSQL محلي داخل بيئة التجهيز، لذلك لم يُنفذ ملف migration فعليًا هنا. لهذا أضيف اختبار قبول بعد النشر يجب تشغيله في Supabase بعد تنفيذ SQL، ولا تُعد المرحلة مقبولة تشغيليًا قبل ظهور نتيجة PASS منه.

## أثر المرحلة على المستخدم

لا تغيير مرئي حاليًا. يبقى المحرك السابق هو مسار الإنتاج إلى أن تكتمل Phase 2-D3 ومرحلة الربط اللاحقة.

## النشر المطلوب

- تنفيذ SQL الجديد مرة واحدة.
- نشر Edge Function الجديدة `assessment-generation-jobs`.
- لا سر جديد.
- لا إعادة فهرسة.
- لا إعادة رفع للمراجع.
- لا إعادة نشر `generate-source-questions` أو `question-visual-jobs` لهذه المرحلة.

## الخطوة التالية

Phase 2-D3: بناء عامل المفردة الذي يقرأ المقطع الحقيقي من الخادم، يتحقق من بصمته، يبني حزمة الدليل والعقد العلمي، يستدعي Gemini للصياغة فقط، يطبع النتيجة محليًا، يربط الدليل خادميًا، ثم يحفظ النتيجة عبر عقد D2.
