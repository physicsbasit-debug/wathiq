# واثق — تثبيت تعليمات الحل الحسابي في محرك V2

## Phase 2-A Fix 2 — Calculation Working Normalization

الإصدار التشغيلي: **`0.0.50`** (لا يتغير في هذا الإصلاح)  
عقد التوليد: **`source-grounded-policy-ai-17-whole-exam-v2`**

يعالج هذا الإصلاح ثاني خلل ظهر في اختبار القبول الحي لمحرك تصميم الاختبار كاملًا: كان السؤال الحسابي يُرفض إذا أعاد Gemini الحقل `workingRequired=false`، حتى لو كانت المعطيات والإجابة ونموذج التصحيح سليمة.

## القاعدة الجديدة

- سؤال حسابي بدرجتين أو أكثر: يثبت واثق `workingRequired=true` تلقائيًا.
- سؤال حسابي بدرجة واحدة: يثبت واثق `workingRequired=false` تلقائيًا.
- لا يعتمد القبول على القيمة التي يقترحها النموذج.
- تضيف واجهة واثق وWord/PDF عبارة **«أظهر خطوات الحل بوضوح»** عندما تكون مطلوبة.
- لا يُسقط الاختبار الكامل بسبب هذا الحقل الشكلي.
- عند أي فشل آخر تبقى المسودة على محرك V2 ولا تتغير طريقة التوليد تلقائيًا.

## الحماية

- بقي محرك V2 والمحرك السابق كما هما.
- لم يتغير عقد التوليد أو SQL أو الفهرسة أو Word/PDF أو الصور.
- لا حاجة إلى دليل المعلم أو إعادة رفع الكتب.

## الفحص

```text
356 passed
0 failed
```

بعد الدمج يجب إعادة نشر:

```text
supabase/functions/generate-source-questions/index.ts
```

## الأساس البصري الموروث

يبقى **Phase 1-C1** ومبدأ **مواصفة SVG حتمية وآمنة** أساس الرسوم العلمية التي تؤثر في صحة الحل. هذا الإصلاح لا يغيّر `fixedVisual` ولا طبقة الصور الهجينة، بل يثبت فقط متى تظهر تعليمات خطوات الحل.
