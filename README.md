# واثق — حفظ المسودات وفحص المدخلات الحسابية

## Phase 2-A Fix 5 — Draft Checkpoints & Calculation Input Integrity

الإصدار التشغيلي: **`0.0.50`** (لا يتغير في هذا الإصلاح)  
عقد التوليد: **`source-grounded-policy-ai-17-whole-exam-v2`**

يعالج هذا الإصلاح مشكلتين ظهرتا في اختبار القبول الحي:

1. فشل سؤال حسابي لأن الحارس كان يفحص القيم والوحدات داخل الرسم وحده، مع أن بعض المعطيات الصحيحة قد تكون موزعة بين نص السؤال والرسم أو الجدول.
2. تخزين مسودة واحدة فقط، بحيث قد يستبدل إنشاء اختبار جديد المسودة السابقة، مع اعتماد الحفظ على مؤقت قصير بدل نقطة استئناف صريحة قبل الطلبات الطويلة.

## القواعد الجديدة

- يفحص واثق القيم والوحدات في **نص السؤال والمثير البصري معًا**.
- سؤال العزم يتطلب قوى ومسافة عن محور الدوران، ولا يمر بقوتين فقط.
- سؤال الضغط يتطلب قوة ووحدة مساحة.
- تحفظ نقطة استئناف مباشرة قبل استدعاء محرك V2، وبعد النجاح، وبعد الفشل.
- يحفظ التطبيق عند إخفاء الصفحة أو مغادرتها.
- يدعم تخزين حتى 12 مسودة محلية مستقلة بدل استبدال آخر اختبار.
- زر «اختبار جديد» لا يحذف المسودة السابقة؛ تبقى في «اختباراتي».
- تهاجر المسودة القديمة تلقائيًا إلى مخزن المسودات المتعددة.

## الحماية

- لا SQL جديد.
- لا إعادة فهرسة.
- لا تغيير في Word أو PDF أو تخزين الصور.
- لا تغيير في الإصدار أو عقد V2.
- لا حاجة إلى رفع دليل المعلم.

## الفحص

```text
380 passed
0 failed
```

بعد الدمج يجب إعادة نشر:

```text
supabase/functions/generate-source-questions/index.ts
```

## التوافق مع محرك الرسومات السابق

يحافظ هذا الإصلاح على أساس **Phase 1-C1** القائم على **مواصفة SVG حتمية وآمنة** وحقول `fixedVisual`، ولا يستبدل محرك الرسومات أو يغير عقده.
