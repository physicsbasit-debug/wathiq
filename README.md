# واثق

## Phase 0-H1 Rebuild 2 — الفهرس البصري بإحداثيات الكلمات

تعيد هذه المرحلة بناء استخراج هيكل الكتاب من صفحة الفهرس المصوّرة نفسها، لا من النص المسطح. تستخدم واثق إحداثيات كل كلمة من Google Cloud Vision لفصل العمود الأيمن عن الأيسر، ثم تربط رموز الدروس بالوحدات وأرقام الصفحات.

### النتيجة المطلوبة للمرجع الحقيقي

- 11 وحدة.
- 29 درسًا.
- الوحدة الأولى موجودة وتبدأ من صفحة 15.
- الوحدة الحادية عشرة تبدأ من صفحة 109.
- لا أرقام صفحات داخل العناوين.
- لا حفظ لأي نتيجة ناقصة أو غير متسلسلة.

### التغييرات

- مسار Edge Function جديد: `ocr-layout-page`.
- تخزين `layout_json` داخل `source_ocr_pages` لإعادة استخدام التحليل دون تكلفة Vision إضافية.
- تحليل صفحة PDF بدقة مرتفعة وبحد آمن للبكسلات.
- فصل العمودين بحسب الإحداثيات الفعلية.
- دعم التفاف عنوان الوحدة أو الدرس على سطرين.
- التحقق من تسلسل الوحدات والدروس وتصاعد أرقام الصفحات قبل الحفظ.

### الفحص

- TypeScript: PASS
- Build: PASS
- Tests: 119/119 PASS
- Edge Function syntax transpile: PASS

ملف `pages.yml` لم يتغير.

## Phase 0-H1 Rebuild 2 Fix 1

يعالج تعليق `ocr-layout-page` عند وصول Supabase إلى `WallClockTime` عبر مهلات صريحة، إعادة محاولة واحدة، إلغاء الطلب من المتصفح، تسجيل مراحل التنفيذ، وتقليل حجم صورة الفهرس. نتيجة الاختبارات الحالية: **126/126 PASS**.


## Phase 0-H1 Rebuild 2 Fix 2 — Cache Handshake

يعالج هذا الإصلاح تعليق التحليل عند وجود `layout_json` محفوظ. أصبح فحص الكاش طلب `GET` خفيفًا بلا صورة، ولا تُرسم صفحة PDF ولا تُرفع إلى Edge Function إلا عند غياب الكاش. كما يستهلك مسار `POST` جسم الصورة قبل أي استجابة مبكرة، ويحافظ على مهلات Fix 1 وإصلاح أنواع معرفات Drive.

- لا يوجد SQL جديد.
- `pages.yml` لم يتغير.
- المرجع الذهبي ما زال 11 وحدة و29 درسًا.

## Phase 0-H1 Rebuild 2 Fix 3 — Spatial Lesson Recovery

يعالج هذا الإصلاح الحالة الفعلية التي نجح فيها OCR الموضعي في قراءة عناوين الوحدات وأرقام الصفحات، لكنه شوّه أو حذف بعض رموز الدروس. لم يعد قبول الدرس متوقفًا على ظهور الرمز بصيغة مثالية؛ يسترد واثق الصف من موضعه داخل الوحدة، وعنوانه العربي، ورقم الصفحة الموجود في طرف الصف، ثم يستنتج رقم الدرس من التسلسل عند الحاجة.

### حالات الانحدار المغلقة

- استرداد دروس الوحدتين الثانية والثالثة عند غياب الرموز بالكامل.
- استرداد الدرس الثاني من الوحدة الرابعة عند فقد رمزه.
- دمج درس ملتف على سطرين في الوحدة الأولى.
- فهم الشرطة العربية الطويلة ورقمي الدرس المنفصلين بلا شرطة.
- استبعاد «مصطلحات علمية» و«ملحق» من قائمة الدروس.
- الحفاظ على الرفض الصارم لأي تسلسل ناقص بعد الاسترداد.

لا يحتاج هذا الإصلاح إلى SQL أو تحديث Edge Function، ولا يغيّر `pages.yml`.
