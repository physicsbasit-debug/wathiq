# واثق — Phase 2-A Fix 1

## إصلاح نطاق الدليل لكل مفردة في محرك V2

الإصدار التشغيلي يبقى **`0.0.50`**، وعقد التوليد يبقى:

```text
source-grounded-policy-ai-17-whole-exam-v2
```

هذا إصلاح محدود لأول خلل ظهر في اختبار القبول الحي. كان الـ prompt يطلب من Gemini اختيار دليل من قائمة المفردة، لكن JSON Schema كان يسمح بجميع مقاطع مراجع الاختبار. لذلك أمكن للنموذج اختيار مقطع صحيح من درس آخر، ثم يرفضه المدقق برسالة:

```text
اختار مولد الأسئلة دليلًا لا ينتمي إلى مرجع المفردة.
```

## الإصلاح

- أصبح `sourceEvidenceId` لكل مفردة مقيدًا داخل JSON Schema بمقاطع `sourceReferenceId` الخاص بها فقط.
- بقي التحقق النهائي الخادمي كما هو بوصفه طبقة أمان ثانية.
- أضيف فشل مبكر إذا كانت أي مفردة بلا مقاطع دليل مرتبطة.
- لم يتغير محرك التخطيط أو Word/PDF أو الصور أو الفهرسة أو SQL.

## الفحص

```text
347 passed
0 failed
```

بعد الدمج يجب إعادة نشر:

```text
supabase/functions/generate-source-questions/index.ts
```

لا يلزم SQL أو سر جديد أو إعادة فهرسة.

## الأساس البصري الموروث

يبقى **Phase 1-C1** ومبدأ **مواصفة SVG حتمية وآمنة** أساس الرسوم العلمية التي تؤثر في صحة الحل. هذا الإصلاح لا يغيّر محرك الرسوم.
