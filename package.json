{
  "name": "wathiq",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "واثق: تجربة المعلم وإدارة المصادر المحلية للمرحلة 0-C",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
