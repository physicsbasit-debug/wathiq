{
  "name": "wathiq-phase-0b-ux-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "نموذج تفاعلي محلي لمرحلة 0-B من منصة واثق",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
