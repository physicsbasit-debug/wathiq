{
  "name": "wathiq",
  "version": "0.1.0-reset-bootstrap",
  "private": true,
  "type": "module",
  "description": "Temporary two-file bootstrap for Wathiq 0.1.0 Quality Reset. Run Repository Maintenance once.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test",
    "repo:check": "node -e \"console.log(\\\"RESET BOOTSTRAP PENDING: run Repository Maintenance\\\")\"",
    "repo:clean": "node WATHIQ_RESET_0_1_0.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
