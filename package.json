{
  "name": "wathiq",
  "version": "0.0.13",
  "private": true,
  "type": "module",
  "description": "واثق: إضافة الفصل الدراسي للمصادر وإصلاح حفظ السجل بعد اكتمال رفع الكتب الكبيرة",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
