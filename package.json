{
  "name": "wathiq",
  "version": "0.3.15",
  "private": true,
  "type": "module",
  "description": "واثق: مولد اختبارات علوم كامبريدج عربي بتأليف عميق ومراجعة مستقلة، يبدأ دورة التوليد مباشرة بعد فحص العقد المحلي ويستخدم أول مفردة حقيقية كبوابة للمزود بدل preflight خارجي حاجب.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run repo:check && npm run legacy:check && npm run ui:check && npm run test",
    "repo:check": "node scripts/check-repository-hygiene.mjs",
    "repo:clean": "node scripts/check-repository-hygiene.mjs --clean",
    "ui:check": "node scripts/check-arabic-rtl.mjs",
    "legacy:check": "node scripts/check-anti-legacy.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
