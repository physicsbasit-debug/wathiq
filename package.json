{
  "name": "wathiq",
  "version": "0.0.24",
  "private": true,
  "type": "module",
  "description": "واثق: إدارة مصادر علمية وفهرستها حسب الصفحات والمقاطع دون اشتراط تحليل فهرس بصري",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
