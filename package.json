{
  "name": "wathiq",
  "version": "0.0.12",
  "private": true,
  "type": "module",
  "description": "واثق: OCR عربي فعلي لملفات PDF مع الاستكمال بعد الانقطاع وبوابة جودة قبل الفهرسة",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/physicsbasit-debug/wathiq.git"
  }
}
