# Phase 2-C3 Fix 1 — Structured Scenario Contract & Per-Item Repair

- الإصدار: 0.0.56
- عقد التوليد: `source-grounded-policy-ai-19-structured-scenario-repair`

## الإصلاح الجذري
- إزالة قائمة `SCENARIO_KEYWORDS` التي كانت ترفض السياقات الصحيحة بسبب اختلاف لفظ واحد.
- إضافة عقد `scenarioContract` إلى JSON المولد، ويحتوي الهدف وعبارات دليل من متن السؤال والرابط العلمي وكون السياق أساسيًا.
- التحقق من وجود عبارات الدليل حرفيًا داخل السؤال، ثم التحقق الدلالي من ارتباطها بالسياق وهدف التعلم والرسم المملوك للخادم.
- فصل التحقق إلى مستوى المفردة، والاحتفاظ بالمفردات الصحيحة وإعادة توليد المفردات المرفوضة فقط.
- إعادة توليد الاختبار كاملًا فقط عند فشل قاعدة عالمية تخص تنوع الاختبار ككل.

## معايير القبول
- لا مطابقة بكلمة واحدة لإثبات السياق.
- لا خسارة للأسئلة السليمة بسبب مفردة واحدة.
- سبب الرفض مرتبط بـ planItemId.
- توافق مؤقت مع عقود ai-17 وai-18 للمسودات السابقة.

## نتيجة الفحص النهائي
- `npm run build`: PASS
- `npm run check`: 409 passed, 0 failed
- لا تغيير في SQL أو منظومة مهام الصور الدائمة.
