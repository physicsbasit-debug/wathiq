# Phase 2-C4 Fix 5 — Server-Owned Assessment Contract & Moment System

## الحالة التي كشفت الخلل

بعد انتظار طويل، انتهى التوليد بثلاثة أنواع من الرفض:

1. سؤال العزم لم يكرر نصيًا محور الدوران وذراع القوة وموضع التأثير.
2. عقد السياق لم يُرجع عبارتين حرفيتين من متن السؤال.
3. نموذج قوى لم يُصنف القوة المؤثرة والاحتكاك بصورة صحيحة.

هذه ليست ثلاثة أخطاء منفصلة. الجذر كان واحدًا:

> الخادم كان يملك الخطة والمرئي والنموذج العلمي، لكنه ظل يعتمد على Gemini في حقول وصفية مشتقة، كما استخدم نموذج `force_system` واحدًا لمسائل مختلفة علميًا، ثم أعاد التوليد عدة مرات بسبب اختلاف هذه الحقول.

---

## الإصلاح الجذري

### 1. عقد السياق أصبح مملوكًا للخادم

أزيل `scenarioContract` من مخطط خرج Gemini. يبنيه الخادم الآن بعد استلام السؤال من:

- `stimulus`
- `text`
- `scenarioTarget`
- هدف التعلم
- `fixedVisual`

تُستخرج عبارات الدليل من النص نفسه، لذلك لا يمكن أن يفشل سؤال صحيح لأن النموذج نسي حقلًا وصفيًا أو أعاد عبارة غير حرفية.

### 2. نموذج مستقل لمسائل العزم

أضيف:

```text
moment_system
```

ويحتوي على:

- `moment_force`
- `lever_arm`
- علاقة `moment`
- اتجاه `clockwise` أو `counterclockwise`
- العزم المحصل بوحدة `N m`

لم تعد مسائل العزم تمر عبر `force_system` الذي يفرض وجود قوة مؤثرة واحتكاك، وهو الشرط الصحيح لعربة على سطح أفقي لكنه عبث علمي عند باب أو أرجوحة أو مفتاح ربط.

### 3. إصلاح تصنيف القوى الحرة

أزيل الاعتماد على ترتيب المتجهات (`index === 0`) لتسمية القوة المؤثرة. أصبح التصنيف قائمًا على المعنى الفعلي للتسمية بعد التطبيع العربي:

- القوة المؤثرة
- الاحتكاك
- الوزن
- رد الفعل

وهذا عالج الخطأ الذي كان يصنف الوزن على أنه القوة المؤثرة.

### 4. تقليل زمن التوليد

كان واثق يصلح نموذج التصحيح بطلب Gemini إضافي **قبل** معرفة أن السؤال نفسه سيمر ببقية بوابات الجودة. أي أنه كان يدفع ثمن إصلاح سؤال سيُرفض بعد لحظات، لأن الوقت فائض لدى البشرية على ما يبدو.

أصبح الآن:

- إكمال نقاط التصحيح الناقصة محليًا من الإجابة والتعليل والقالب الرسمي.
- تعليمها `needsReview=true` عند الإكمال المحلي.
- لا استدعاء Gemini ثانٍ لمجرد عدد نقاط التصحيح.
- خفض الحد الأقصى لمحاولات الاختبار الكامل من 4 إلى 3.

هذا لا يلغي إصلاح المحتوى الحقيقي، لكنه يزيل الجولات الناتجة عن بيانات وصفية أو نموذج تصحيح ناقص.

---

## الإصدار

```text
0.0.61
source-grounded-policy-ai-23-server-owned-assessment-contract
```

---

## نتيجة الفحص

```text
npm run check
PASS: TypeScript compiled and static assets copied to dist/
439 passed
0 failed
```

كما تم تشغيل `dist` محليًا وكانت الاستجابة:

```text
HTTP 200
```

---

## النشر المطلوب

بعد الدمج، يُعاد نشر وظيفة واحدة فقط:

```text
supabase/functions/generate-source-questions/index.ts
```

لا يلزم:

- SQL جديد
- سر جديد
- إعادة فهرسة
- إعادة نشر `question-visual-jobs`
- إعادة رفع الكتاب أو دليل المعلم

---

## اختبار القبول الحي

بعد النشر:

1. نفذ `Ctrl + F5`.
2. افتح المسودة نفسها.
3. اضغط **التالي**.
4. راقب أن مفردة العزم لا تطلب احتكاكًا.
5. راقب أن فشل `scenarioContract` اختفى.
6. تحقق أن سؤال القوة الحرة يحمل القوة المؤثرة والاحتكاك بالقيم الصحيحة.
7. قارن زمن التوليد؛ يجب ألا تظهر جولة مستقلة لإصلاح نقاط التصحيح.

الاتصال الحي بـGemini/Supabase لم يكن متاحًا في بيئة التجهيز، لذلك هذا التوليد هو اختبار القبول التشغيلي النهائي.
