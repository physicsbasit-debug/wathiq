# Phase 2-C4 Fix 2 — Server-Owned Scientific Item Contract

## الإصدار
- التطبيق: `0.0.59`
- عقد التوليد الجديد: `source-grounded-policy-ai-21-server-owned-scientific-item`
- تستمر وظيفة Edge في قبول عقد `ai-20` للمسودات الحالية، لكنها تعيد بناء النموذج العلمي خادميًا عند إعادة المحاولة.

## الجذر المعالج
كان التوليد الكامل والإصلاح الانتقائي يعتمدان على أن يعيد Gemini حقل `scientificItem`. عند جولات الإصلاح كان النموذج قد يعيد السؤال من دون الحقل، فيرفضه الخادم رغم صحة بقية البنية.

## التصميم الجديد
1. يبني الخادم `serverScientificItem` حتميًا من خطة المفردة والمرئي المملوك له.
2. يرسل النموذج إلى Gemini للقراءة فقط ضمن `batchPlanItems`.
3. لا يطلب مخطط النقل من Gemini إعادة `scientificItem`.
4. يستخدم `hydrateGeneratedItem` النموذج الخادمي نفسه في الجولة الأولى وكل جولات الإصلاح الانتقائي.
5. تبقى بوابات الاتساق العلمي بين النص والإجابة والمرئي كما هي.
6. تم تصحيح عقد الشحن بالدلك ليكون الجسمان بشحنتين متعاكستين.
7. تُحسب القوة المحصلة واتجاهها خادميًا من متجهات القوى التي يملكها واثق.
8. تستخدم المسودات الحالية بعقد `ai-20` المسار الخادمي نفسه عند الضغط على «التالي» بعد النشر.

## نتائج الفحص
- `npm run build`: PASS
- `npm run check`: **427 passed, 0 failed**
- اختبار overlay فوق Phase 2-C4 Fix 1: **427 passed, 0 failed**
- اختبار النسخة الكاملة: **427 passed, 0 failed**
- فحص TypeScript مستقل لوظيفة `generate-source-questions`: PASS
- تشغيل `dist` محليًا: HTTP 200
- فحص الملفات المحظورة: لا `.env` ولا `.git` ولا `node_modules`

## النشر
بعد الدمج يعاد نشر `supabase/functions/generate-source-questions/index.ts` فقط.
لا يوجد SQL جديد ولا تعديل على `question-visual-jobs`.
