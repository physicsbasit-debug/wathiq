# Phase 2-D3 — تقرير عامل توليد المفردة

## النتيجة

أُنجز عامل مستقل يولد مفردة واحدة فوق منظومة D2 الدائمة. يقرأ المقطع الحقيقي من Supabase، ويتحقق من بصمته وملكيته، ويبني العقد العلمي والمرئي الحتمي، ثم يستخدم Gemini لصياغة المحتوى فقط ويحفظ النتيجة داخل المهمة قبل إعادتها.

المسار الجديد لا يستدعي المحرك السابق، ولا ينتظر تحسين الصور، ولم يُربط بعد بواجهة الإنتاج.

## ما أضيف

### نواة المحرك

- `source-grounding.ts` لتنظيف المصدر وتقسيمه واختيار الدليل خادميًا.
- `scientific-contracts.ts` لبناء العقود العلمية والمرئيات الحتمية.
- `item-validation.ts` للتحقق من الارتباط العلمي والنتيجة ونموذج التصحيح.
- بصمة مستقلة لمحتوى المصدر في `hashing.ts`.
- تصدير الوحدات الجديدة من `assessment-engine/index.ts`.

### عامل Edge

أضيفت وظيفة:

```text
assessment-generation-worker
```

وتنفذ:

```text
claim → grounding → Gemini → normalization → validation → complete/fail
```

وتستخدم حجز D2 ونبضه ومنع نتائجه القديمة.

### خدمة العميل

أضيفت `AssessmentGenerationWorkerService` بعمليات:

- `health`.
- `processItem`.
- `processItemSynchronously`.

الخدمة غير مستوردة في `app.ts`، لذلك لا يتغير مسار المستخدم في هذه المرحلة.

### المرئي العلمي

يبني عقد D3 مشهد العزم السياقي داخل النواة الجديدة، بما يشمل محور الدوران وموضع تأثير القوة وذراع القوة والقيم المطابقة، دون تعديل مدقق المحرك الإنتاجي السابق في هذه المرحلة.

## حماية المصدر

- يقرأ العامل `source_registry` و`source_chunks` للمالك نفسه.
- يتحقق من عنوان المصدر ونوعه وإصدار الاستخراج والصفحات.
- يعيد حساب `source_content_hash` ويرفض المصدر المتغير.
- يعيد حساب `contract_hash` ويرفض العقد المتغير.
- ينظف السطور الشبيهة بحقن المطالبات قبل إرسال المصدر للنموذج.
- لا يرسل معرفات الأدلة إلى Gemini؛ يختارها الخادم بعد التوليد.

## حدود دور Gemini

مخطط النموذج يحتوي سبعة حقول فقط:

```text
stimulus, text, options, answer, rationale, markScheme, needsReview
```

ولا يحتوي هويات الخطة أو المصدر أو الدليل أو المرئي أو العقد العلمي.

## نتائج الفحص

- `npm run build`: ناجح.
- `npm run check`: **484 passed, 0 failed**.
- فحص TypeScript الصارم لوظيفة Edge: ناجح.
- اختبار تركيب الحزمة فوق D2: ناجح.
- تشغيل `dist` محليًا: **HTTP 200**.
- اختبار العقود العلمية للعزم: ناجح.
- اختبار تنظيف حقن المطالبات وربط الدليل: ناجح.
- اختبار رفض إجابة تخالف النتيجة الحتمية: ناجح.
- اختبار استقلال العامل عن المحرك السابق ومسار الصور: ناجح.
- اختبار خدمة العميل دون ربط `app.ts`: ناجح.
- فحص ZIP والأسرار والمسارات المحظورة: ناجح.

## ما لم يمكن التحقق منه محليًا

لم يتوفر اتصال حي بمشروع Supabase أو Gemini في بيئة التجهيز، لذلك لم تُشغل مفردة حقيقية عبر الوظيفة المنشورة. نشر العامل يتم بعد دمج الحزمة، أما اختبار التوليد المتكامل فيصبح عمليًا ضمن ربط Phase 2-D4 بالدورات الحقيقية.

## أثر المرحلة على المستخدم

لا تغيير مرئي حاليًا، ولا يزال زمن التوليد الحالي كما هو لأن واجهة الإنتاج لم تتحول بعد إلى العامل الجديد. هذا مقصود: D3 تبني العامل وتثبت حدوده، وD4 تربطه بالواجهة والتقدم والاستكمال.

## النشر المطلوب

- رفع الحزمة ونجاح GitHub Actions بنتيجة `484 passed, 0 failed`.
- نشر Edge Function الجديدة `assessment-generation-worker`.
- لا SQL جديد.
- لا سر جديد.
- لا إعادة فهرسة أو رفع مراجع.
- لا إعادة نشر `generate-source-questions` أو `question-visual-jobs`.

## الخطوة التالية

Phase 2-D4: بناء منسق الواجهة الذي ينشئ دورة التوليد، ويشغل مهمتين بالتوازي، ويستعيد التشغيل بعد تحديث الصفحة، ويدمج النتائج الجاهزة في المسودة، ويعيد المفردة الفاشلة وحدها، ثم يمرر الاختبار إلى المراجعة العامة.
