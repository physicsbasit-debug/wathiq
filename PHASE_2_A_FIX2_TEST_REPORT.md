# Phase 2-A Fix 2 — تقرير الفحص

## النطاق

إصلاح رفض الأسئلة الحسابية بسبب قيمة `workingRequired` فقط، مع تثبيت تعليمات خطوات الحل تلقائيًا حسب عدد الدرجات، والحفاظ على وضع محرك V2 عند فشل محاولة التوليد.

## اختبارات مضافة

- تطبيع سؤال حسابي من درجتين من `false` إلى `true` في Edge Function.
- تطبيع سؤال حسابي من درجة واحدة من `true` إلى `false` في Edge Function.
- الاختباران نفسيهما في محلل المحرك السابق.
- الاختباران نفسيهما في محلل Whole Exam V2.
- فحص نصي يمنع عودة رسالة الرفض القديمة ويثبت بقاء المسودة على V2.

## النتيجة

```text
356 passed
0 failed
```

## فحوص الحزمة

- البناء ناجح.
- اختبار overlay فوق Phase 2-A Fix 1 ناجح.
- اختبار النسخة الكاملة بعد فكها ناجح.
- تشغيل `dist` محليًا يعيد HTTP 200.
- لا أسرار، ولا `.env`، ولا SQL، ولا تعديل على GitHub workflows.
