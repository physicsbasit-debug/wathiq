# تعليمات نشر Phase 2-C3

## الترتيب إلزامي

### 1. رفع ملفات GitHub

ارفع محتويات حزمة `changed_files_only` فوق فرع جديد من `main` الحالية، مع الاستبدال.

اسم الفرع المقترح:

`phase-2c3-durable-visual-assets`

رسالة الحفظ:

`feat: add durable visual asset pipeline`

انتظر GitHub Actions. النتيجة المطلوبة:

`406 passed, 0 failed`

لا تدمج قبل ظهور الأخضر.

### 2. تنفيذ SQL مرة واحدة

بعد الدمج، افتح:

`Supabase > SQL Editor > New query`

انسخ كامل محتوى الملف:

`supabase/phase_2_c3_visual_asset_jobs.sql`

ثم اضغط `Run`.

ينشئ هذا جدول مهام الصور الدائمة وسياسة الحماية الخاصة به.

### 3. إنشاء Edge Function الجديدة

الاسم يجب أن يكون حرفيًا:

`question-visual-jobs`

من Supabase:

`Edge Functions > Deploy a new function > Via Editor`

انسخ محتوى:

`supabase/functions/question-visual-jobs/index.ts`

ثم انشر الوظيفة.

لا تحذف وظيفة:

`generate-source-questions`

فالوظيفة الجديدة تستخدمها بوصفها محرك التوليد والفحص العلمي.

### 4. الأسرار

لا يوجد سر جديد. الوظيفة تستخدم الأسرار الموجودة أصلًا:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `WATHIQ_APP_URL`

### 5. تحديث التطبيق

بعد اكتمال نشر GitHub Pages:

`Ctrl + F5`

### 6. اختبار القبول

أنشئ اختبارًا جديدًا يحتوي على:

- سؤال شحن بالدلك أو تجاذب/تنافر.
- سؤال بمشهد حياتي.
- سؤال مخطط قوى لعربة أو حقيبة.

المتوقع:

- تظهر حالة المهمة: طابور، توليد، فحص، جاهز.
- يمكن مغادرة الصفحة ثم العودة وتستمر الحالة من Supabase.
- لا يسمح واثق بالاعتماد أو التصدير قبل جاهزية الصور المطلوبة.
- المشهد يظهر كصورة 2D حقيقية.
- مخطط القوى يظهر بصورة 2D للكائن مع الأسهم والقيم فوقها.
- الصورة نفسها تظهر في المعاينة وWord وPDF.

## لا يلزم

- إعادة فهرسة الكتب.
- رفع دليل المعلم.
- سر جديد.
- تعديل وظيفة `generate-source-questions` في هذه المرحلة.
