# Phase 2-C4 Fix 1 — Lean Gemini Schema & Server-Owned Validation

## الجذر
أدى توسيع النموذج العلمي الموحد إلى تضخم `responseJsonSchema`: استخدم مخططًا موضعيًا `prefixItems` لكل مفردة، وحدودًا ديناميكية للمصفوفات، وتعدادات متغيرة للأدلة والمعرفات داخل بنية عميقة. Gemini قد يرفض المخططات الكبيرة أو شديدة التعقيد قبل بدء التوليد.

## الإصلاح المعماري
- فصل **مخطط النقل** عن **قواعد المجال**.
- Gemini يستقبل مخططًا ثابتًا وخفيفًا يضمن شكل JSON الأساسي فقط.
- الخادم يملك كل القيود الدقيقة: عدد المفردات والبدائل، معرفات الخطة، الأدلة المسموحة، نقاط التصحيح، السياق، النموذج العلمي، الأرقام والوحدات والاتجاهات.
- تبسيط مخطط إصلاح نموذج التصحيح بالطريقة نفسها.

## ما لم يتغير
- عقد `ai-20`.
- النموذج العلمي الموحد.
- بوابات النزاهة العلمية.
- مهام الصور الدائمة.
- SQL أو الأسرار.

## نتائج الفحص
- `npm run build`: PASS
- `npm run check`: **422 passed, 0 failed**
- فحص TypeScript مستقل لوظيفة `generate-source-questions`: PASS

## النشر
يكفي إعادة نشر `generate-source-questions` فقط. لا SQL جديد ولا تعديل على وظيفة مهام الصور.
