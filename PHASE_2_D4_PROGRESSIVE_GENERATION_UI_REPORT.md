# Phase 2-D4 — تقرير واجهة التوليد التدريجي والاستكمال

## النتيجة التنفيذية

انتقلت واجهة إعداد الاختبار من انتظار استجابة جماعية واحدة إلى دورة توليد دائمة تتعامل مع كل مفردة بوصفها مهمة مستقلة. تبني الواجهة الخطة الرسمية أولًا، تنتقل مباشرة إلى صفحة التقدم، تنشئ أو تستعيد دورة D2، ثم تشغل عامل D3 بحد أقصى مهمتين متزامنتين.

كل مفردة جاهزة تُدمج فورًا في المسودة وتُحفظ محليًا، ولا تنتظر اكتمال بقية الاختبار. عند تحديث الصفحة أو إغلاقها، تستعيد الواجهة الدورة من Supabase وتكمل المهام غير المنتهية دون إعادة الأسئلة الجاهزة.

## التحويل الإنتاجي

أصبح `src/app.ts` يستخدم في مسار إعداد الاختبار:

- `AssessmentGenerationJobService` لإدارة الدورة والمهام الدائمة.
- `AssessmentGenerationWorkerService` لتشغيل عامل المفردة.
- `ProgressiveAssessmentGenerationOrchestrator` للتوازي المحدود والاستكمال.
- `buildProgressiveGenerationPayload` لبناء مخطط وعقود مستقلة ذات مصادر صريحة.

لم يعد التطبيق يستدعي إنتاجيًا:

- `QuestionGenerationService`.
- `generateWholeExam`.
- الإرسال الجماعي للاختبار كاملًا.
- جولات إصلاح شاملة تعيد الأسئلة الناجحة.
- التحويل التلقائي إلى `legacy_items` عند كبر الاختبار.

تظل ملفات المحرك السابق موجودة مؤقتًا لدعم قراءة المسودات التاريخية والاختبارات المرجعية، لكنها خارج مسار التوليد الجديد في `app.ts`. إزالتها الفيزيائية مؤجلة إلى مرحلة القطع والتنظيف بعد نجاح اختبار القبول الحي.

## تجربة المستخدم الجديدة

بعد تثبيت إعداد الاختبار والضغط على التالي:

1. تُبنى الخطة الرسمية وتحفظ.
2. تنتقل الواجهة فورًا إلى صفحة التوليد والمراجعة.
3. يظهر شريط تقدم حقيقي وعدد المكتمل والمتعذر والنشط والمنتظر.
4. تظهر حالة كل مفردة ومرحلتها وعدد محاولاتها.
5. تعمل مهمتان فقط بالتوازي.
6. تظهر المفردة الجاهزة فور وصولها، وتُحدد تلقائيًا بوصفها الصياغة المعتمدة للمفردة.
7. يمكن إعادة المفردة المتعذرة وحدها.
8. يمكن إلغاء الدورة دون حذف النتائج التي اكتملت وحُفظت.
9. بعد اكتمال جميع المفردات تبدأ مهام الصور المحسنة خارج المسار الحرج.

## الاستكمال بعد تحديث الصفحة

يحفظ كل Draft:

- `generationRunId`.
- `generationEpoch`.
- نمط `progressive_items_v1`.

وعند فتح المسودة أو تسجيل الدخول ومزامنة المصادر:

- تقرأ الواجهة الدورة الحالية من Supabase.
- تتحقق من تطابق `generationEpoch` و`planHash` و`sourceSnapshotHash`.
- تدمج النتائج الجاهزة الموجودة خادميًا.
- تعيد تشغيل المهام المنتظرة أو القابلة للمحاولة فقط.
- لا تعيد إرسال أي مفردة حالتها `ready`.

عند تعديل الخطة أو المصدر، تُلغى الدورة القديمة وترتفع الإزاحة؛ لذلك لا تستطيع نتيجة متأخرة الكتابة فوق خطة أحدث.

## حماية التشغيل

أضيفت حمايات تشغيلية قبل إنشاء الدورة:

- فحص `health` لعامل D3.
- رفض العامل إذا لم يطابق `engineSchemaVersion = 1` و`contractVersion = 1`.
- منع حلقة استدعاء سريعة عند تعذر الشبكة عبر إبقاء مهلة التهدئة بعد خطأ العامل.
- إيقاف المنسق المحلي عند تسجيل الخروج.
- إيقاف المنسق وإلغاء الدورة عند حذف المسودة النشطة.
- فحص هوية المسودة والإزاحة قبل دمج أي لقطة متأخرة.

## بناء العقود والمصادر

ينشئ `assessment-generation-progressive.ts` عقدًا مستقلًا لكل مفردة، ويثبت:

- `planItemId` والدرس والهدف والدرجات ونوع السؤال والمستوى.
- `sourceId` و`chunkIndex` ونطاق الصفحات وإصدار الاستخراج.
- بصمة محتوى المصدر الفعلية.
- العائلة العلمية والمتطلبات والمرئي الحتمي.
- مفاتيح التنوع والسياق والنمط والبذرة العددية.

لا تشترك مفردتان في مرجع ضمني، ولا يستطيع العامل رؤية مصادر المفردات الأخرى داخل عقد المهمة.

## المرئيات والمراجعة العامة

- تُحمى المرئيات الضرورية علميًا، مثل مشاهد العزم، قبل توزيع الميزانية البصرية.
- يستخدم السؤال أولًا SVG العلمي الحتمي.
- لا يبدأ تحسين الصور إلا بعد اكتمال الأسئلة.
- تفحص المراجعة العامة تكرار النصوص والسيناريوهات والبيانات والمرئيات.
- لا تعيد المراجعة الاختبار كاملًا؛ تضع `needsReview` على المفردة المتعارضة لتبقى المراجعة البشرية صريحة.

## التوافق

المسودات التاريخية المكتملة تظل قابلة للعرض والتصدير والاعتماد. يحافظ التخزين على الأنماط التاريخية لأغراض القراءة فقط، بينما كل مسودة جديدة أو غير مكتملة تستخدم `progressive_items_v1`.

لم تتغير عقود:

- Word.
- PDF.
- اعتماد الاختبار.
- عرض ورقة الطالب ونموذج الإجابة.
- `question-visual-jobs`.

## نتائج الفحص

- الإصدار: `0.0.66`.
- `npm run build`: ناجح.
- `npm run check`: **492 passed, 0 failed**.
- TypeScript: ناجح ضمن البناء.
- تشغيل `dist` محليًا: **HTTP 200**.
- اختبار العزل الدقيق للمصادر: ناجح.
- اختبار حماية مرئي العزم: ناجح.
- اختبار التوازي بحد أقصى مهمتين: ناجح.
- اختبار عدم إعادة `ready`: ناجح.
- اختبار الاستكمال بعد الصفحة: ناجح.
- اختبار الانتقال الفوري إلى صفحة التقدم: ناجح.
- اختبار فحص صحة العامل: ناجح.
- اختبار إيقاف المنسق عند الخروج والحذف: ناجح.
- اختبار مهلة التهدئة بعد خطأ الاتصال: ناجح.
- فحص عدم وجود استدعاء إنتاجي للمحرك السابق داخل `app.ts`: ناجح.

## ما لا يمكن التحقق منه محليًا

لا تتوفر في بيئة التجهيز جلسة حقيقية لمشروع Supabase أو مفتاح Gemini الخاص بالمستخدم، لذلك يبقى اختبار القبول الحي إلزاميًا بعد النشر. نجاح الاختبارات يثبت العقود والتدفق والعزل، لكنه لا يختلق زمن استجابة Gemini من الهواء، لأن السحر ليس ضمن `devDependencies`.

## متطلبات النشر

لا يحتوي D4 على SQL أو تعديل لوظائف Edge. يلزم:

- رفع ملفات D4 ونجاح GitHub Actions بـ`492 passed, 0 failed`.
- نشر GitHub Pages بعد الدمج.
- التأكد أن `assessment-generation-jobs` من D2 منشورة.
- التأكد أن `assessment-generation-worker` من D3 منشورة.
- لا إعادة نشر `generate-source-questions`.
- لا إعادة نشر `question-visual-jobs`.
- لا إعادة فهرسة أو رفع مراجع.
- لا سر جديد.

## معيار الإغلاق الحي

تُغلق D4 تشغيليًا بعد نجاح اختبار فعلي يثبت:

- الانتقال الفوري لصفحة التقدم.
- تشغيل مهمتين كحد أقصى.
- ظهور أول سؤال قبل اكتمال الجميع.
- تحديث الصفحة ثم استعادة الدورة دون فقد المكتمل.
- عدم إعادة أي مفردة جاهزة.
- إمكانية إعادة المفردة المتعذرة وحدها.
- اكتمال الاختبار وبدء مهام الصور لاحقًا.
- استمرار Word وPDF والاعتماد دون تغير.
