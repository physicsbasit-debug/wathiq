# Phase 2-A Fix 1 — Per-Item Evidence Scope

## السبب الجذري

كان `generationSchema` يستقبل قائمة عالمية بجميع معرفات الأدلة، ويستخدمها enum لحقل `sourceEvidenceId` في كل مفردة. في الاختبار الكامل متعدد الدروس استطاع النموذج اختيار دليل تابع لمفردة أخرى. المدقق النهائي اكتشف ذلك ورفض الاختبار، لكن الرفض كان متأخرًا بعد استهلاك التوليد وإعادة المحاولة.

## العلاج

- تمرير `EvidenceCatalog` إلى مخطط الاستجابة.
- اشتقاق `allowedEvidenceIds` لكل مفردة من `requestedItem.sourceReferenceId`.
- إبقاء دعم القائمة القديمة في الدالة للاختبارات والمسار المتوافق، بينما مسار التشغيل الفعلي يستخدم الكتالوج المقيد.
- الاحتفاظ بالمدقق النهائي وعدم تخفيفه.

## النطاق

لا تعديل في SQL أو الفهرسة أو الواجهة أو Word/PDF أو الصور أو نموذج Gemini.
