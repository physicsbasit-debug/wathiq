# Phase 2-A — Test Report

## النطاق

- الأساس: Phase 1-D2 Fix 1، الإصدار `0.0.49`.
- الإصدار الجديد: `0.0.50`.
- عقد التوليد الجديد: `source-grounded-policy-ai-17-whole-exam-v2`.

## الاختبارات

```text
345 passed
0 failed
```

يغطي الفحص:

- TypeScript strict build.
- إنشاء بطاقات الدروس التشغيلية من أهداف الخطة ومراجع الكتاب أو الدليل.
- بناء مخطط الاختبار الكامل مع الدرجات والمهارات والسياقات والمثيرات.
- إنشاء طلب `generate_exam_v2` للاختبار كاملًا.
- قراءة سؤال نهائي واحد لكل مفردة وتطبيقه على الخطة.
- منع تكرار نص السؤال أو مجموعة البيانات العددية داخل الاختبار.
- حفظ وضع التوليد في المسودة وترحيل المسودات القديمة بأمان.
- بقاء المحرك السابق متاحًا وعدم حذف مساراته.
- توافق اختبارات المراحل السابقة من Phase 0 إلى Phase 1-D2 Fix 1.
- ضبط Edge Function على مخرج واحد لكل مفردة في V2 وثلاثة بدائل في المحرك السابق.
- بقاء ملكية مواصفة الرسم للخادم وخطة الرجوع الحتمية.

## اختبارات الحزمة المنفذة

- مشروع العمل: `345 passed`.
- تركيب حزمة `changed_files_only` فوق النسخة الأصلية `0.0.49`: `345 passed`.
- فك حزمة `full_backup` في مجلد مستقل: `345 passed`.
- تشغيل `dist` محليًا: `HTTP 200`.
- فحص ZIP: لا `.env`، ولا مفاتيح خاصة، ولا `.git`، ولا `node_modules`.

## القيد المعروف

لم يتوافر اتصال حي بمفتاح Gemini ومشروع Supabase الفعلي في بيئة التجهيز. لذلك يجب بعد الدمج وإعادة نشر Edge Function إنشاء اختبار قصير جديد لا يتجاوز 12 مفردة. نجاح الاختبارات الآلية يثبت العقود والحماية والتوافق، لكنه لا يضمن وحده جودة كل مخرج احتمالي من النموذج.
