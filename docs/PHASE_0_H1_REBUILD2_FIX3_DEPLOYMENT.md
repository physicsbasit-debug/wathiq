# نشر Phase 0-H1 Rebuild 2 Fix 3

1. أنشئ فرعًا من `main` باسم `fix/phase-0-h1-spatial-lesson-recovery`.
2. ارفع محتويات حزمة Changed Files Only مع الحفاظ على المسارات.
3. استخدم رسالة commit: `fix: استرداد دروس الفهرس عند تشويه رموز OCR`.
4. افتح Pull Request وانتظر نجاح CI.
5. بعد الدمج انتظر نشر GitHub Pages ثم نفّذ تحديثًا قسريًا للصفحة.

لا يوجد SQL جديد، ولا يلزم نشر Edge Function، و`pages.yml` لم يتغير.
