# بوابة قبول جودة واثق

نجاح الاختبارات البرمجية يثبت سلامة النظام، **ولا يثبت جودة السؤال**. لا يعلن أن مخرجات واثق توازي أدوات الذكاء الاصطناعي العامة قبل اجتياز هذه البوابة حيًا.

## مجموعة القياس

أنشئ 12 مفردة علوم على الأقل باستخدام **المرحلة/السيلابس + الموضوع فقط**:

- Cambridge Primary: أربع مفردات موزعة بين المراحل 1–6.
- Cambridge Lower Secondary: أربع مفردات تشمل المرحلتين 7 و8 ومفردة من 9.
- Cambridge IGCSE: أربع مفردات من الفيزياء والكيمياء والأحياء أو العلوم المجمعة/المنسقة.

يجب أن تتضمن المجموعة مزيجًا من: اختيار من متعدد، إجابة قصيرة بصور متنوعة، إجابة طويلة عميقة، تفسير/استنتاج من بيانات أو شكل، حساب علمي عند ملاءمته، ومرئي أو مخطط في السياق الذي يخدم الإجابة. لا تُفرض حصة صور عددية.

## معايير حاسمة

1. لا خطأ علمي.
2. ملاءمة واضحة لبرنامج كامبريدج والمرحلة/السيلابس والموضوع.
3. صياغة عربية طبيعية ومهنية.
4. قابلية حل كاملة من المعطيات والمعرفة المتوقعة للمستوى.
5. مستوى تفكير حقيقي، لا مجرد شارة وصفية: التطبيق يوظف المعرفة في موقف/تمثيل جديد، والاستدلال يعالج دليلًا أو علاقة للوصول إلى استنتاج أو تبرير أو تقييم.
6. مشتتات الاختيار من متعدد معقولة ومبنية على أخطاء مفاهيمية محتملة.
7. الإجابة ونموذج التصحيح متسقان مع الدرجة.
8. تنوع حقيقي بين المفردات، لا تغيير أرقام أو قصة سطحية فقط.
9. المرئي، إن اختاره المؤلف، يساهم فعلًا في الإجابة أو توضيح السؤال، ويُنتج ويُراجع علميًا؛ لا يُحذف لمجرد أن النص يمكن قراءته بدونه ولا يُضاف للزينة.
10. الجداول والرسوم البيانية والقيم الرقمية دقيقة وقابلة للتحقق.
11. لا تظهر أحفورة واجهة أو رسالة مزود إنجليزية أو اتجاه LTR غير مقصود.
12. لا يحتاج المستخدم إلى مصدر خارجي حتى ينجح التوليد.

## المقارنة المرجعية

لنفس البرنامج والمرحلة/السيلابس والموضوع ونوع السؤال، أنشئ مرجعًا مباشرًا بأداة ذكاء اصطناعي عامة قوية. الهدف ليس النسخ، بل التأكد أن هندسة واثق لا تخفض جودة المؤلف.

أي خطأ علمي جوهري، مرئي مضلل، أو فرق نوعي واضح لمصلحة الأداة المرجعية = **FAIL**. عند الفشل يعود العمل إلى Author/Reviewer قبل إضافة أي ميزة جديدة.

### بوابة قبول المزود والعقود v0.3.15
- يجب ألا يوجد نداء Gemini تمهيدي قبل `enqueue` أو `resume`; `health` يفحص Worker + Runtime Contract فقط، وأول مفردة حقيقية هي بوابة المزود.
- 400 لا يجوز أن يظهر MODEL_INVALID_JSON.
- 403 يجب أن يظهر MODEL_AUTH_FAILED.
- 404 يجب أن يظهر MODEL_NOT_FOUND.
- MAX_TOKENS يجب أن يظهر MODEL_OUTPUT_TRUNCATED.
- لا يجوز أن يحتوي author/reviewer schema على حقول هندسة المرئيات الثقيلة مثل `vectors/anchors/segments/dimensions/series/components`.
- يجب أن تكون مخططات Visual Planner متخصصة حسب النوع؛ force schema لا يحمل حقول circuit والعكس.
- يجب ألا يُستدعى Visual Planner المنظم عند `none` أو `illustration_2d`.
- يجب أن يحدث Visual Planner بعد اعتماد المراجع، لا قبلها.
- يجب أن يستمر Runtime Contract v0.3.12 دون SQL إضافي في v0.3.15.

- يجب أن يفشل health قبل بدء الدورة إذا غاب Runtime Contract لقاعدة البيانات.
- يجب ألا تنتج `defer_assessment_generation_item_v1` حالة `failed` لأي رمز نقل مؤقت.

### بوابة Visual Planner v0.3.15
- لا يوجد `responseJsonSchema` في طلب `visual_planner`.
- يجب أن يكون الإخراج JSON صالحًا، ثم يمر عبر normalize + validateContent محليًا قبل الحفظ.
- فشل مخطط مرئي يؤثر على المفردة التي طلبته فقط، ولا توجد بوابة preflight للمزود تحجب الدورة.
