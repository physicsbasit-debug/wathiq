# معايير قبول Phase 0-G Fix 2B

- يظهر زر «تشغيل OCR العربي» للمصدر الذي حالته `يحتاج OCR`.
- تُحفظ نتيجة كل صفحة في `source_ocr_pages`.
- يمكن تحديث الصفحة واستكمال الصفحات الناقصة فقط.
- لا يعاد إرسال الصفحات المكتملة.
- يجتاز النص العربي السليم بوابة الجودة ويصبح المصدر `مفهرس`.
- يفشل النص الرديء بأمان ولا يُكتب في `source_chunks`.
- لا يظهر مفتاح Google Vision في ملفات الواجهة أو GitHub.
- تنجح اختبارات البناء والـTypeScript والاختبارات الآلية.
