# Phase 1-C4 — تقرير الاختبار

## النسخة

- إصدار التطبيق: `0.0.45`.
- إصدار التوليد: `source-grounded-policy-ai-13-trusted-enrichment`.
- الأساس: Phase 1-C3 recovery `0.0.44`.

## النتائج

```text
299 tests
299 passed
0 failed
```

## نطاق الفحص

- نجاح TypeScript وبناء `dist`.
- بقاء اختبارات جميع المراحل السابقة خضراء.
- ظهور خيار الإثراء وحفظه في المسودة والطلب.
- عدم حذف الأسئلة المكتملة عند تغيير الخيار.
- تصفية نطاقات المصادر الرسمية والجامعية.
- استخراج الجمل الموثقة من `groundingMetadata` ورفض مصدر عام غير موثوق.
- تعريب خطأ الضغط والمهلة.
- تثبيت ثلاث محاولات نقل فقط ومنع تكرارها في دورة خارجية ثانية.
- قبول ارتباط السؤال بالمرجع الكامل عند ضيق المقطع، مع وسم المراجعة.
- استمرار رفض سؤال لا يرتبط بالمقطع ولا بالمرجع الكامل.
- حفظ أثر المصدر الخارجي ورفض رابط ببروتوكول غير آمن في الواجهة.
- ترقية مسودة C3 المكتملة دون حذف الخطة أو البدائل.
- ثبات المرئيات والتصدير والاختيار من متعدد من Phase 1-C3.

## اختبارات الحزمة

- فُكّت حزمة الملفات المعدلة فوق النسخة الأساسية ثم شُغّل الفحص كاملًا.
- فُكّت النسخة الكاملة الاحتياطية في مجلد مستقل ثم شُغّل الفحص كاملًا.
- شُغّل `dist` محليًا وتحقق رد HTTP 200.
- فُحصت الحزم للتأكد من عدم وجود `.git` أو أسرار أو ملفات بيئة.
