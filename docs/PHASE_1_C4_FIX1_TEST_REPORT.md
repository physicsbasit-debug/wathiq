# Phase 1-C4 Fix 1 — تقرير الاختبار

## النسخة

- إصدار التطبيق: `0.0.46`.
- إصدار التوليد: `source-grounded-policy-ai-14-contextual-stimulus-alignment`.
- الأساس: Phase 1-C4 `0.0.45`.

## النتيجة

```text
306 tests
306 passed
0 failed
```

## اختبارات الإصلاح

- قبول سؤال بصري ذي `stimulus` فارغ عندما يحمل الرسم أو الجدول البيانات ويشير النص إليه صراحة.
- استمرار رفض السؤال السياقي المبهم إذا غاب المتن والرسم والسياق المضمن.
- قبول إشارات: الشكل، الرسم، المخطط، الدائرة، الجدول، التدريج، الجهاز، البيانات الممثلة، والتمثيل.
- تمرير سبب رفض المحاولة الأولى إلى المحاولة الثانية.
- نجاح المحاولة الثانية بعد تصحيح المتن الناقص.
- ترقية مسودة C4 الجزئية إلى العقد الجديد دون حذف المفردات المكتملة.
- عزل دفعة المفردتين عند الفشل وحفظ المفردة السليمة منفردة.
- بقاء الإثراء الموثوق والمرئيات والتصدير وWord وPDF والاختيار من متعدد دون تغيير وظيفي.

## فحوص القبول

- نجاح TypeScript وبناء `dist`.
- نجاح جميع اختبارات المراحل السابقة.
- اختبار الحزمة الفعلية فوق Phase 1-C4 في مجلد مستقل.
- اختبار النسخة الكاملة بعد فك الضغط.
- تشغيل `dist` محليًا والتحقق من HTTP 200.
- فحص الحزم من ملفات الأسرار و`.env` و`.git`.
