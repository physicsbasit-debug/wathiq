# Phase 2-C4 Fix 4 — Moment Evidence Context

## المشكلة

بعض مفردات العزم كانت تُرفض برسالة:

> سؤال العزم في الموقف الحياتي لا يحدد محور الدوران أو ذراع القوة أو موضع تأثيرها بصورة تسمح بتبرير الإجابة علميًا.

مع أن هذه المعلومات كانت موجودة فعلًا في `fixedVisual` المملوك للخادم، خصوصًا في مخطط العزم (`force_diagram` / `moments`) من خلال:

- `title`
- `purpose`
- `altText`
- `annotations`
- القيم والمسافات المرفقة

لكن التحقق كان يعتمد على **نص السؤال وحده تقريبًا**، فينتج رفض كاذب (false negative):
السؤال صحيح، والمرئي صحيح، لكن البوابة لا تنظر إلا إلى الكلمات الموجودة حرفيًا في `stimulus + text`.

---

## الحل الجذري

تم نقل تحقق العزم من منطق "كلمات في نص السؤال فقط" إلى منطق "دليل مركب مملوك للخادم" يجمع:

- متن السؤال (`stimulus` + `text`)
- `fixedVisual`
- `serverScientificItem`
- تلميحات خادمية مضبوطة للمشهد عند وجود مخطط عزم حقيقي

أصبح الخادم يقبل المفردة إذا توفر الدليل العلمي عبر **السؤال أو الشكل أو العقد العلمي الخادمي**، وليس فقط من الجملة النصية.

وبقي الرفض قائمًا عندما يغيب الدليل من الجميع معًا.

---

## التعديلات البرمجية

### 1) supabase/functions/generate-source-questions/index.ts
- إضافة `momentScenarioHintText(...)`
- إضافة `buildMomentValidationCorpus(...)`
- توسيع `validateAssessmentQuality(...)` لتستقبل:
  - `fixedVisual`
  - `scientificItem`
- تحويل تحقق العزم إلى تحقق مركب يعتمد على:
  - محور/ارتكاز
  - ذراع/بعد/مسافة عمودية
  - موضع تأثير القوة
- إضافة توجيه صريح في Prompt يسمح بالاكتفاء بالإشارة إلى الشكل عندما يكون `fixedVisual` نفسه يحمل تفاصيل العزم اللازمة.

### 2) tests/gemini-generate-content-edge.test.mjs
- إضافة اختبار يثبت قبول سؤال عزم حياتي عندما يوفر الشكل المرفق دليل الارتكاز وذراع القوة دون تكرارها كلها في النص.
- إضافة اختبار يثبت استمرار الرفض عندما يغيب الدليل من النص والشكل معًا.
- تصدير helper داخلي للاختبار المباشر.

### 3) tests/phase-2-c4-fix3-server-owned-question-pattern.test.mjs
- تحديث التوقع النصي ليتوافق مع التوقيع الجديد لـ `validateAssessmentQuality(...)` بعد تمرير `fixedVisual` و`scientificItem`.

---

## نتيجة الفحوص

تم تشغيل:

```bash
npm run check
```

والنتيجة:

- `PASS: TypeScript compiled and static assets copied to dist/`
- `432 passed, 0 failed`

---

## الأثر العملي

هذا الإصلاح يعالج الحالة التي ظهرت في الصورة المرفوعة للمستخدم: سؤال عزم صحيح دلاليًا ومرئيًا، لكنه رُفض لأن البوابة كانت عمياء عن بيانات الشكل.

الآن:

- **لن يُرفض السؤال فقط لأنه لم يكرر نصيًا كل عناصر العزم** ما دام `fixedVisual`/`serverScientificItem` يقدمان الدليل العلمي.
- **لن نضعف الجودة**؛ إذا اختفى الدليل من السؤال والشكل معًا سيبقى الرفض موجودًا.
