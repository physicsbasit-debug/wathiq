# Phase 2-C4 Fix 7 — Essential Scientific Visual Contract

## النتيجة

تم إصلاح السبب الجذري لفشل مفردات العزم مثل `plan-2` و`plan-5`، وليس تخفيف رسالة التحقق أو استثناء المفردة من الجودة.

- إصدار التطبيق: `0.0.63`
- عقد التوليد: `source-grounded-policy-ai-25-essential-scientific-visual-contract`
- الاختبارات: `449 passed, 0 failed`
- بناء TypeScript: ناجح
- فحص Edge Function الصارم: ناجح
- تشغيل `dist` محليًا: `HTTP 200`

## السبب الجذري المؤكد

كان `applyWholeExamVisualBudget()` يفرض سقفًا عدديًا للرسوم. في اختبار من ست مفردات يسمح بأربعة رسوم فقط، ثم يحول الرسوم الأقل أولوية إلى `visualTarget: "none"`.

هذا السلوك أسقط مرئي سؤال العزم نفسه، بينما بقيت بوابة الجودة الخادمية تطلب وجود:

1. محور الدوران أو نقطة الارتكاز.
2. ذراع القوة أو البعد العمودي.
3. موضع تأثير القوة.

إعادة إنتاج الحالة قبل الإصلاح أعطت:

```text
OLD:
plan-2 -> none
```

وبعد الإصلاح:

```text
NEW:
plan-2 -> context_scene
```

أي إن طبقة التنسيق كانت تحذف الدليل العلمي ثم ترفض السؤال لغياب الدليل الذي حذفته. تم إلغاء هذا التعارض البنيوي.

## الإصلاح البنيوي

### 1. حماية المرئي الضروري في الواجهة

أضيف عقد يحدد أن مرئي العزم ضروري علميًا عندما تكون المفردة سياقية أو حسابية أو بياناتية أو تقيس تطبيقًا/استدلالًا.

- المرئي الضروري يُحجز أولًا قبل توزيع بقية ميزانية الرسوم.
- لا يتحول إلى `none` حتى لو تجاوز عدد المرئيات الضرورية السقف التجميلي.
- إذا وصلت مفردة قديمة بلا مرئي، تُرقّى حتميًا إلى:
  - `context_scene` للسياق الحياتي.
  - `force_diagram` للمسألة الحسابية.
  - `data_table` لمفردة البيانات.

### 2. حارس خادمي مستقل

لا يعتمد الإصلاح على سلامة الواجهة وحدها. قبل بناء الموجه واستدعاء Gemini، تعيد Edge Function فرض العقد العلمي على:

- `officialPlanItems`
- `items`
- `blueprint.items`

ويُستدل على مفهوم العزم من:

- اسم الدرس والهدف.
- موضوع الاختبار.
- نص المرجع المرتبط بالمفردة.
- بطاقة الدرس ومفاهيمها وملخصها.

لذلك تعمل الحماية حتى مع مسودة قديمة أو طلب مخفض وصل وفيه `visualTarget: "none"`.

### 3. إبقاء بوابة الجودة صارمة

لم تُحذف بوابة محور الدوران وذراع القوة وموضع التأثير، ولم يُضف استثناء يسمح بمرور سؤال غامض. بدلًا من ذلك أصبح الخادم يضمن وجود المرئي العلمي الصحيح قبل التوليد، ثم تظل البوابة تتحقق من النتيجة.

## الملفات التشغيلية المعدلة

- `package.json`
- `src/assessment-generation-v2.ts`
- `supabase/functions/generate-source-questions/index.ts`
- `dist/assets/assessment-generation-v2.js`
- `dist/assets/assessment-generation-v2.js.map`

أضيفت اختبارات انحدار للحالة نفسها، وحدّثت اختبارات توافق الإصدارات لقبول `0.0.63` والعقد `ai-25`.

## فحوص القبول

- سؤال عزم سياقي قديم بـ`none` يُرقّى إلى `context_scene`.
- سؤال عزم عام العنوان يُكتشف من نص المرجع.
- مسألة عزم حسابية قديمة تُرقّى إلى `force_diagram`.
- ميزانية أربعة رسوم لا تسقط مرئي `plan-2` الضروري.
- المرئيات الضرورية تبقى حتى إذا زاد عددها على سقف التنسيق.
- الخادم يعيد فرض المرئي قبل Gemini ويضيف محور الدوران وموضع التأثير وذراع القوة.
- جميع اختبارات المشروع: `449/449`.

## النشر المطلوب

بعد دمج الحزمة:

1. نشر GitHub Pages.
2. إعادة نشر `supabase/functions/generate-source-questions/index.ts` فقط.
3. تحديث قاسٍ للمتصفح `Ctrl + F5`.
4. فتح المسودة نفسها والضغط على **التالي**.

لا يلزم SQL أو سر جديد أو إعادة فهرسة أو إعادة نشر `question-visual-jobs`.

## حد التحقق

لم يتوفر اتصال حي بحساب Supabase أو Gemini داخل بيئة التجهيز، لذلك أول إعادة توليد بعد النشر هي اختبار القبول الحي النهائي. أما البناء والعقد الخادمي واختبارات الانحدار والـoverlay فقد تم التحقق منها محليًا.
