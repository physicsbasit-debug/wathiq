# Phase 2-C4 Fix 6 — Context-Aware Moment Contract

## الحالة التي ظهرت

بعد نجاح مطابقة الخطة للمرجع، فشل إصلاح `plan-5` بالرسالة:

```text
سؤال العزم في الموقف الحياتي لا يحدد محور الدوران أو ذراع القوة أو موضع تأثيرها بصورة تسمح بتبرير الإجابة علميًا.
```

## التشخيص الجذري

الخلل لم يكن في المرجع ولا في خطة الاختبار، ولم يكن قابلًا للحل بمجرد مطالبة Gemini بمحاولة أخرى.

كان هناك تعارض داخل عقد واثق نفسه:

1. الخادم أنشأ `context_scene` عامًا للموقف الحياتي، مثل باب ومقبض.
2. بوابة الجودة اشترطت ثلاثة عناصر علمية صريحة لمسألة العزم:
   - محور الدوران أو نقطة الارتكاز.
   - ذراع القوة أو المسافة عن المحور.
   - موضع تأثير القوة.
3. المرئي ثابت ومملوك للخادم، لذلك لا يستطيع Gemini إضافة العناصر الناقصة إليه خلال جولة إصلاح المفردة.
4. النتيجة كانت تكرار الإصلاح ثم رفض المفردة الصحيحة بعد انتظار طويل. آلة تعاقب نفسها ثم تسلّم المستخدم الفاتورة، تقليد برمجي عريق.

## الإصلاح المنفذ

### 1. اكتشاف مفهوم العزم من الخطة

أضيف كشف خادمي يعتمد على عنوان الدرس وهدف التعلم، ويتعرف على مفاهيم مثل:

- عزم القوة.
- الاتزان الدوراني.
- محور الدوران.
- ذراع القوة.
- نقطة الارتكاز.

### 2. إثراء مشاهد العزم قبل استدعاء Gemini

عندما تكون المفردة عن العزم ومرئيها `context_scene`، يبني الخادم مرئيًا علميًا محددًا بدل المشهد العام.

أصبحت المشاهد المعروفة تحمل دلالات صريحة، ومنها:

- الباب: المفصل محور الدوران، والمقبض موضع تأثير القوة، والمسافة بينهما ذراع القوة.
- الأرجوحة: نقطة الارتكاز محور الدوران، والمقعد موضع تأثير القوة.
- مفتاح الربط: الصامولة محور الدوران، وطرف المفتاح موضع تأثير القوة.
- مكبح الدراجة: مفصل المكبح محور الدوران، والمقبض موضع تأثير القوة.
- عربة التسوق: محور العجلات ومحور تأثير القوة عند المقبض عند تناول العزم.

تُحفظ هذه المعلومات في:

- `purpose`
- `title`
- `altText`
- `labels`
- `annotations`

وبذلك يقرأها مولد السؤال، وبوابة الجودة، ومسار العرض والتصدير من المصدر نفسه.

### 3. توسيع مادة التحقق المنظمة

أصبحت بوابة العزم تقرأ أيضًا محتوى المرئيات الجدولية:

- `tableColumns`
- `tableRows`
- `tableCells`

وهذا يمنع رفض سؤال عزم يعتمد على جدول يثبت القوة والمسافة، لمجرد أن البيانات ليست في نص السؤال الحر.

### 4. عقد توليد جديد متوافق مع السابق

الإصدار الجديد:

```text
0.0.62
source-grounded-policy-ai-24-context-aware-moment-contract
```

ويستمر الخادم في قبول عقود الاختبار الكامل السابقة، ومنها `ai-23`، حتى لا تُكسر المسودات الموجودة.

## ما لم يتغير

- لا SQL جديد.
- لا أسرار جديدة.
- لا إعادة فهرسة للكتاب.
- لا تعديل لوظيفة `question-visual-jobs`.
- لا تغيير في معادلات خطة الاختبار أو توزيع الدرجات.
- لا إعادة توليد لبقية المفردات السليمة بسبب هذا الإصلاح.

## نتيجة الفحص

```text
npm run check
PASS: TypeScript compiled and static assets copied to dist/
442 passed
0 failed
```

وتم تشغيل نسخة `dist` محليًا:

```text
HTTP 200
```

## النشر المطلوب

بعد الدمج:

1. اترك GitHub Pages يعيد بناء الواجهة من المستودع.
2. أعد نشر Edge Function الحالية من الملف:

```text
supabase/functions/generate-source-questions/index.ts
```

لا تنشئ وظيفة جديدة.

## اختبار القبول الحي

الاتصال الحي بـGemini وSupabase غير متاح في بيئة التجهيز، لذلك يلزم اختبار قبول واحد بعد النشر:

1. نفذ تحديثًا قويًا للصفحة `Ctrl + F5`.
2. افتح المسودة نفسها.
3. اضغط **التالي** لإعادة محاولة المفردة المتعثرة.
4. تحقق من عدم عودة رسالة نقص محور الدوران أو ذراع القوة أو موضع التأثير.
5. راجع السؤال والرسم علميًا قبل الاعتماد النهائي.
